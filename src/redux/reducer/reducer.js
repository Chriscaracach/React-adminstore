//Reducer de redux

//Declaramos un estado inicial de nuestra Store, en éste caso, dos arrays que van a contener los elementos para ser mostrados
const initialState = {
  //Estado inicial
};

export const Reducer1 = (state = initialState, action) => {
  switch (action.type) {
    //reducers
    default:
      return state;
  }
};

//Agregar otros reducers
